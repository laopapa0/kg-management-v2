# 数据指标知识图谱管理平台 v2 — 领域上下文

## 项目概述

简化版数据指标知识图谱管理 B 端后台。聚焦四大核心：指标挂靠（指标树+标签集）、血缘画布、配置规则、知识库管理。砍掉审核，支持深色/浅色主题切换。

## 技术栈

- React 19 + TypeScript 5.9 + Vite 7
- Tailwind CSS + shadcn/ui
- React Router v7
- localStorage 持久化（Demo 级别）
- 深色/浅色主题（CSS 变量 + class 切换）

## 四大主菜单

| 菜单 | 说明 |
|------|------|
| **指标管理** | 核心页面：指标树 + 标签集 + 指标关联。中间放待选指标，四周放挂靠目标 |
| **知识库管理** | 文件级维护，版本替换，无审核。业务部门直接录入 |
| **血缘画布** | 沿用 v1 SVG 实现，简化操作，主题颜色联动 |
| **报告管理** | 原巡检改造。筛选 + 模板 + 版本管理 + 反哺 |

## 核心领域术语

| 术语 | 定义 |
|------|------|
| **指标（Indicator）** | 业务数据指标，如"5G用户渗透率"。平表实体，包含编码、名称、一级、二级、颗粒度等字段。同时也是指标树的节点（含 treeParentId 构成层级） |
| **待挂靠指标** | 页面中间区域展示的指标列表。treeParentId 为空且 tagIds 为空且 ruleIds 为空时，显示在待挂靠区域 |
| **指标树（Indicator Tree）** | 指标的层级父子关系（通过 treeParentId 构成），部门级维护。一对一归属 |
| **标签集（Tag Set）** | 可无限层级维护的标签树。同级标签一行并列显示，多选。指标通过 tagIds 关联（多对多） |
| **规则（Rule）** | 业务规则（如阈值告警、波动检测）。树状呈现，多对多关联。指标挂靠后需设置具体参数 |
| **指标挂靠（Attachment）** | 将待选指标通过连线方式关联到指标树/标签集/规则的操作 |
| **血缘关系（Lineage）** | 指标之间的依赖、因果、聚合等关系。选中指标后列出所有关联，点击可聚焦高亮 |
| **关联关系类型（Link Relation Type）** | 血缘关系的类型定义层（meta schema）。定义关系类型的语义（名称、编码、方向）、视觉属性（颜色、图标）和适用范围（源/目标对象类型）。血缘画布中的每一条连线实例都引用一个关联关系类型 |
| **知识库（Knowledge Base）** | 文档容器。有权限的人直接录入维护，无需审核 |
| **知识文档（Knowledge Document）** | 业务部门上传的文件。新文件导入后审查相似度，直接替换原文件对应部分 |
| **报告管理（Report Management）** | 原巡检功能改造。报告计划 + 筛选范围 + 模板 + 版本管理 |
| **反哺（Feedback Loop）** | 从报告中直接修改关联关系或更新知识库，修改后重新跑报告生成新版本 |
| **分权分域（Department Scope）** | 账号登录后自动切换到对应部门的指标树和标签集。规则配置全局可见 |
| **主题（Theme）** | 深色/浅色切换。演示优先用深色（蓝黑）。画布图表颜色跟随主题联动 |
| **指标脑图（Indicator Mind Map）** | 指标树的思维导图可视化视图。使用 Mind Elixir 5 渲染，与列表树视图通过 Toggle 切换。同一份 attachmentStore 数据，两种渲染方式 |
| **默认虚拟分组（Default Virtual Group）** | 指标树和脑图共享的根级虚拟分组节点。在脑图视图中作为连线挂靠投递的目标区域，接收从候选指标池投递来的指标，用户再从此处拖拽指标挂入主树 |
| **NOC（Network Operation Center）** | 运营中心。大屏接口返回的特殊部门编码，NOC 用户 role='admin'，可跨部门查看和操作所有数据。NOC 部门创建的报告模板对所有部门可见（作为全局模板） |
| **报告实例（Generated Report）** | 报告档案。由报告计划生成的一个持续存在的报告记录，`created_at` 即 v1 生成时间，包含 `latest_version` 指向当前最新版本 |
| **报告版本（Report Version）** | 报告实例的每次执行结果。对同一报告实例点击"重新跑"会生成新的报告版本（v1, v2, v3...），每个版本对应一个 HTML 文件路径 |
| **AI 推荐管理（AI Recommendation）** | 关系管理页的第二个 Tab。展示 AI 推荐的关系列表，按置信度排序。支持的交互：勾选应用到血缘画布、置信度筛选、来源指标部门/目标指标部门筛选、关系类型筛选。前端与模型解耦，先用 mock 数据验证验收流程 |
| **关系最后改动者（Last Modified By）** | 关联关系管理列表中每条关系记录的最终修改来源。值为人类操作者姓名（如张三/李四）或 `AI`。用于 ALL/HUMAN/AI 三维筛选和列表行底部的 BY xxx 标签 |
| **发散分析（Divergence Analysis）** | 报告计划的附加选项。启用后报告生成时执行发散文案分析，结果与主报告物理隔离，放在报告末尾。UI 上通过提示词控制分析方向 |
| **菜单三大模块** | v2 新菜单结构。一级手风琴折叠分组：指标图谱（指标管理+血缘画布）、报告管理（报告计划+历史+模板）、基础维护（规则管理+关联关系管理[含AI推荐]+知识库管理）。首页在分组之外独立展示 |

## 已确认的关键决策

### 1. 页面布局（4 面板）

```
┌─────────────────┬───────────────┬─────────────────┐
│   指标树         │               │    标签集        │
│  (左上)         │   待选指标      │   (右上)         │
│                 │  (中间核心)     │   (树状并列)      │
│   (可展开)       │               │                 │
│                 │               │    规则          │
│                 │               │   (右下)         │
│                 │               │   (树状)          │
└─────────────────┴───────────────┴─────────────────┘
```

### 2. 数据结构

```typescript
interface Indicator {
  id: string;
  name: string;
  code: string;
  // 业务字段...
  
  treeParentId?: string;  // 指标树中的父节点（一对一归属）
  tagIds: string[];       // 关联的标签（多对多）
  ruleIds: string[];      // 关联的规则（多对多）
}
```

- 指标树通过 `treeParentId` 关系渲染
- 标签集通过 `tagIds` 关联（独立 TagNode 存储层级）
- 规则通过 `ruleIds` 关联（独立 Rule 存储配置）

### 3. 连线式挂靠交互

1. 点击待挂靠指标 → 进入"连线模式"
2. SVG 虚线 + 箭头从指标指向鼠标位置，实时跟随
3. 鼠标悬停目标节点时，节点高亮
4. 按 **空格键** 确认挂靠
5. 按 **ESC** 取消

### 4. 标签集树交互

- 树状呈现，同级标签一行并列显示
- **空格** = 选中/取消选中当前标签
- **Ctrl+Alt+Shift+空格** = 全选当前层级所有标签并退出多选模式
- 卡片形状展示

### 5. 规则树交互

- 树状呈现，多对多关联
- 指标挂靠到规则后，需要设置具体参数（如阈值上下限）
- 参数设置方式待定（弹窗/侧边栏/内嵌）

### 6. 分权分域

- Header 头像栏加部门切换器
- 默认选中"财务部"
- 指标树和标签集随部门切换
- 规则区域全局不变
- 待选指标全局可见

### 7. 主题

- 先只做深色主题（蓝黑）
- 浅色后续迭代

### 8. 删除挂靠

- 方式待定（右键菜单/hover删除按钮/连线点击删除）

### 9. 树节点维护

- 添加节点：面板标题栏 "+" 按钮
- 编辑名称：inline 编辑
- 删除节点：hover 显示删除按钮
- 拖拽调整层级：指标 treeParentId 变更

## 已废弃（v1 → v2）

| 旧术语 | 状态 | 说明 |
|--------|------|------|
| 审核 | ❌ 已砍掉 | 所有模块取消审核流程 |
| 巡检 | ❌ 已改造 | 升级为报告管理 |
| NOC | ❌ 已隐藏 | 运营侧不需要的功能全部删除 |
| 预览 | ❌ 已砍掉 | 血缘画布预览功能删除 |
| 映射/底座 | ❌ 已隐藏 | 运营侧不需要 |
| 对象类型（Object Type）| ⚠️ 已简化 | v1 中的复杂对象类型体系简化为指标平表字段 |

## 编码约定

- 文件组织：`src/pages/<module>/<Component>.tsx`
- 测试文件：`src/pages/<module>/<Component>.test.tsx`
- 工具文件：`src/utils/<feature>.ts`
- 模型文件：`src/models/<model>.ts`
- localStorage key 前缀：`kgv2-`（与 v1 区分）
- **sticky 搜索栏/统计行**：使用 `bg-dark-card-l1/95 backdrop-blur-sm z-20`，确保滚动时遮挡下方内容不穿透、不被树节点动画层级覆盖
- **GitHub Pages 自动部署**：push 到 `main` 分支后，`.github/workflows/deploy.yml` 自动触发（vite build → upload-pages-artifact → deploy-pages）。`vite.config.ts` 中 `base: '/kg-management-v2/'`，因此生产 URL 为 `https://laopapa0.github.io/kg-management-v2/`。部署约需 1-2 分钟，用户说的"生成新页面"即指此流程。
