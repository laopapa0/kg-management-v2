# 数据指标知识图谱管理平台 — 领域上下文

## 项目概述

数据指标知识图谱管理平台是一个 B 端管理后台，用于管理数据指标的对象实例、血缘关系、标签、业务规则等元数据，支持业务部门操作、NOC 管理和平台维护三大模块。

## 核心领域术语

| 术语 | 定义 |
|------|------|
| **指标（Indicator）** | 业务数据指标，如"5G用户渗透率"。是多个对象类型实例的组合实体，在代码中表现为平表实体 |
| **对象类型（Object Type）** | 指标的属性字段定义，如一级、二级、颗粒度、指标标识、指标值单位、屏、对接部门、指标业务口径概述、指标技术口径、关注频率。每个对象类型有自己的属性（字段）和实例枚举值 |
| **对象类型实例（Object Type Instance）** | 对象类型的具体值，如"经营"（一级对象类型的实例）、"收入"（二级对象类型的实例） |
| **血缘关系（Lineage）** | 指标之间的依赖、因果、聚合等关系，构成图谱的边 |
| **标签（Tag）** | 打在指标上的分类标记，如"核心指标"、"集团考核" |
| **规则（Rule）** | 业务规则，如阈值告警、波动检测、TOPN监控等 |
| **规则模板（Rule Template）** | 规则的参数化模板，如"阈值上下限"、"波动算法"、"TOPN监控" |
| **NOC** | Network Operations Center，网络运营中心，负责元数据审核和管理 |
| **PRD** | Product Requirements Document，产品需求文档 |
| **ADR** | Architecture Decision Record，架构决策记录 |
| **知识库（Knowledge Base）** | 文档容器，分为「默认业务知识库」（系统预设，唯一）和「专业知识库」（NOC 创建）。业务部门上传文档时需指定目标知识库 |
| **知识文档（Knowledge Document）** | 业务部门上传到知识库的文件，经 NOC 审核通过后嵌入知识库。与现有 `KnowledgeDoc` 接口对应，新系统是对其生命周期的完整管理 |
| **文档分段块（Document Chunk）** | 知识文档按分段参数切分后的文本块，用于嵌入和相似度检测 |
| **审核状态** | 知识文档的生命周期状态：编辑中 → 待审核 → 审核中 → 已通过 / 审核不通过。审核不通过可重新编辑提交 |
| **指标管理** | 业务部门的指标生命周期管理页面，整合「新增指标」「我的指标」「变更指标」三个 Tab |
| **大屏来源** | 指标的数据源属性，如「统一数据门户」「经营管理大屏」「网络运营大屏」「客户服务大屏」。指标数据通过大屏系统导出导入 |
| **指标属性结构图** | 可视化组件，以指标为中心节点，放射状连接其所属对象类型分组（分类属性、指标值单位、屏、对接部门等），展示指标与对象类型之间的组合关系 |

### ⚠️ Flagged ambiguities

| 旧术语/用法 | 状态 | 说明 |
|------------|------|------|
| 对象类型 = 指标的分类维度（如经营/发展/交付/服务） | ❌ **已废弃** | 原错误理解，把 Excel 的列值当成了树层级分类。经营/发展等是一级对象类型的实例值，不是分类维度本身 |
| 对象类型 = 指标本身 | ⚠️ **已修正** | 原 PRD 中的简化说法不够精确。正确关系：指标是多个对象类型实例的组合实体，对象类型是属性字段定义 |
| 指标名字（作为对象类型名） | ❌ **已废弃** | 更名为"指标标识"，避免与"指标名称"单个字段混淆 |
| 属性管理（独立页面） | ⚠️ **已合并** | 0602 会议确认属性管理失去独立维护意义，属性作为对象类型的字段定义嵌入对象类型管理中 |

## 业务规则配置交互模式（已确认）

### 视图结构
- **左侧**：三级规则目录树（L1 规则大类 → L2 规则子类 → L3 规则模板）
- **右侧**：按模板筛选的**指标列表**（非规则列表）
- **底部**：全局操作栏（规则统计 + "确认发布到图谱"按钮）

### 核心交互
1. 点击左侧 L3 模板 → 右侧显示**应用了该模板的所有指标**
2. 右侧表格每行 = **一个指标**（去重），列：指标名称、编码、分类、单位、状态、操作
3. 点击指标行 → **展开**显示该指标下属于当前模板的所有规则及其参数
4. 展开区内可"编辑规则"（弹窗）或"删除规则"
5. "添加新规则"→ 弹窗，弹窗内保留 L1→L2→L3 级联选择流程
6. 弹窗内"保存"= 本地保存；底部"确认发布到图谱"= 全局生效提交

### 已确认
- 右侧默认显示**所有指标**（包括未配规则的），从 `availableIndicators` 列表渲染
- 左侧模板树作为**筛选器**，点击后右侧过滤为"应用了该模板的指标"，支持清除筛选回到全部
- 搜索框：左侧搜分类/模板，右侧搜指标名
- 底部操作栏保留"确认发布到图谱"作为全局提交按钮

### 已确认
- 展开后规则详情用**内嵌小表格**展示
  - 未选中模板时列：规则名称、模板类型、参数、状态、操作
  - 选中模板后列：规则名称、参数、状态、操作（模板已知）
- 弹窗内保留**知识文档**和**业务日历**字段
- 弹窗内 L1→L2→L3 级联选择使用独立 state，不依赖页面选中状态

## 巡检管理交互模式（已确认）

### 页面结构
- **顶部 Tab 切换**：`当前巡检计划` | `巡检结果`，参考平台维护「图谱管理/数据源管理」Tab 样式
- **默认进入**：当前巡检计划 Tab
- **新建巡检计划**：通过页面内按钮展开单页长表单（不跳转路由）

### 核心实体
- **巡检计划（InspectionPlan）**：统一实体，含触发类型（`periodic` 定期 / `rule-based` 规则触发 / `manual` 手动）
- **巡检执行（InspectionExecution）**：计划每次执行的记录（时间、状态、覆盖指标数、异常数）
- **巡检报告（InspectionReport）**：基于执行生成的产物（异常明细、趋势图、处置建议、血缘影响）

### 已确认
- **定期触发**：按周/月固定周期执行全量巡检
- **自动触发（自定义）**：配置异常规则作为触发器，指标命中规则时立即执行专项巡检
- **手动触发**：预设规则/过滤/范围后保存为可复用计划，按需手动执行
- **图谱版本**：UI 显示 `v2.3.1`，仅一个选项，无实际版本管理功能
- **指标范围筛选**：基于对象类型值 或 基于标签（如"黄金指标"）
- **异常规则过滤（排除）**：左侧规则三级树 + 右侧已选排除列表双栏模式
- **血缘影响面**：复用现有血缘画布组件，只读精简版，高亮上游根因/当前异常/下游波及
- **处置建议**：知识库匹配（`knowledgeDocs`）作为 LLM prompt 上下文，调用 DeepSeek-V4-Pro API 生成，API key 由 `.env` 注入
- **巡检质量评分**：异常发现率 × 60% + (1 − 误报率) × 40%
- **报告推送**：仅展示"站内消息通知"模拟，点击后弹出"发送成功"提示，邮件和企微/钉钉不做
- **报告状态**：整体三级状态——正常（无异常）、异常（有未处理异常）、已处理（全部异常已确认/处理）
- **趋势对比图**：当前周期 vs 上一周期 vs 基线，复用现有 `recharts` 依赖

## 技术栈

- React 19 + TypeScript 5.9 + Vite 7
- Tailwind CSS 3.4 + shadcn/ui (Radix UI)
- Framer Motion（动画）
- Lucide React（图标）
- React Router v7（路由）
- Recharts（图表，已存在）

## 模块结构

### 业务部门
- **指标管理**（新增）— 整合新增/变更/管理指标的入口页面，含「我的指标」（默认）/「新增指标」/「变更指标」三个 Tab
- 血缘画布（可视化编辑指标间关系）
- 配置标签
- 配置业务规则
- **巡检待办**（新增）— 接收 NOC 推送的巡检报告，对异常项做「是否误报」二元评价

### NOC 管理
- 对象类型 / 链接关系 / 属性 / 标签 / 规则管理
- 审核待办
- **巡检管理**（新增）

### 平台维护
- 对象类型 / 链接类型 / 属性 / 图谱管理

---

## 业务部门巡检待办（新增）

### 核心概念
| 术语 | 定义 |
|------|------|
| **巡检待办报告** | `InspectionReport` 的业务视图，复用同一数据结构，新增 `businessReview` 评价状态 |
| **评价状态** | `pending`(待评价) → `saved`(已保存草稿) → `submitted`(已提交反馈) |
| **二元评价** | 每个异常项选「是误报」或「非误报」（确认异常），可选填备注 |
| **弹窗结构** | **上方**：完整报告详情（复用 NOC 侧组件：概览卡片、质量评分详情、异常统计、异常表格、趋势对比、血缘影响面、处置建议）；**下方**：评价操作区（异常表格行内 radio + textarea + 【保存】按钮），上下独立 |
| **保存 ≠ 提交** | 弹窗内【保存】仅存 localStorage 草稿；列表【提交】才正式反馈给 NOC |

### 评价数据持久化
- 评价草稿存储在 `localStorage`
- 提交后更新内存中的 `InspectionReport.businessReview`，误报标记参与评分公式
- Demo 级别，无后端 API

### 状态颜色
| 状态 | 颜色 |
|------|------|
| 待评价 | 灰色 |
| 已保存 | 黄色 |
| 已提交 | 绿色 |

---

## 知识库管理（新增）

### 核心概念
| 术语 | 定义 |
|------|------|
| **默认业务知识库** | 系统初始化时自动创建，所有业务部门上传的默认目标。NOC 不可删除 |
| **专业知识库** | NOC 通过【创建空知识库】创建，供业务部门选择。名称全局唯一 |
| **分段参数** | 文档上传时的文本切分配置：分段标识符、最大长度、重叠长度、预处理规则 |
| **相似度检测** | NOC 审核时将待审文档与知识库中已嵌入文档比对，≥ 80% 时系统提示建议 |
| **嵌入** | 审核通过后，将文档内容按分段参数切分并加入知识库的过程（Demo 级别仅为状态变更） |

### 状态流转
```
编辑中 ──[提交审核]──→ 待审核 ──[NOC 开始审核]──→ 审核中
                                                        │
        ┌───────────────────────────────────────────────┘
        │
    [通过并嵌入] ──→ 已通过
    [审核不通过] ──→ 审核不通过 ──[重新编辑]──→ 编辑中
```

### 数据持久化（Demo 级别）
- 知识库列表、文档元数据（名称/状态/上传人/时间等）→ **localStorage**
- 文件原始内容、分段块列表 → **内存/mock**
- 表单草稿（分段参数）→ **localStorage**（刷新保留，文件需重新上传）

### 界面路由
| 角色 | 菜单位置 | 页面结构 |
|------|---------|---------|
| 业务部门 | 配置业务规则 → **知识上传** → 巡检待办 | Tab：上传新文档 / 我的文档 |
| NOC | 审核待办 → **知识管理** → 巡检管理 | Tab：知识库管理 / 知识审核 |

---

## 指标管理（新增）

### 核心概念
| 术语 | 定义 |
|------|------|
| **我的指标** | 业务部门用户创建/提交过的指标申请列表，含状态跟踪（编辑中 → 待审核 → 已通过 / 被驳回） |
| **指标属性结构图** | 以指标为中心的可视化图谱，放射状展示该指标所属的全部对象类型分组及其字段值。区别于「血缘画布」（展示指标与指标之间的关系） |

### 状态流转
```
编辑中 ──[提交审核]──→ 待审核 ──[NOC 审核中]──→ 已通过 / 被驳回
                                                    │
                              被驳回后可重新编辑 ←──┘
```

### 数据持久化（Demo 级别）
- 指标申请/变更记录元数据 → **localStorage**（`indicatorStorage.ts`）
- 指标属性结构图数据 → 从 `OBJECT_TYPE_DEFINITIONS` + `Indicator` 实例实时推导
- Demo 级别，无后端 API

### 界面路由
| 角色 | 菜单位置 | 页面结构 |
|------|---------|---------|
| 业务部门 | **指标管理** → 血缘画布 | Tab：我的指标（默认）/ 新增指标 / 变更指标 |
