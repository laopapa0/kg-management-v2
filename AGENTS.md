# Agent 指引

## Agent skills

### Issue Tracker

Issues 和 PRDs 存放在本仓库的 GitHub Issues 中。See `docs/agents/issue-tracker.md`.

### Triage 标签

使用默认 canonical labels。See `docs/agents/triage-labels.md`.

### Domain 文档

项目根目录 `CONTEXT.md` + `docs/adr/` 目录。See `docs/agents/domain.md`.

## 交互约定

- 所有推理、规划和回复必须使用中文。
- 代码注释可以用英文，但对用户的解释用中文。
