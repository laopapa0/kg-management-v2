# 🔬 调研提示词 — 指标挂靠核心页面交互设计

> 目标：为 v2 核心页面"指标挂靠"（指标树 + 标签集 + 规则）设计完整、人性化的交互方案。
> 技术栈：React 19 + TypeScript + Tailwind CSS + shadcn/ui + SVG
> 主题：深色（蓝黑），演示优先

---

## 一、页面布局（已确认）

```
┌─────────────────┬───────────────┬─────────────────┐
│   指标树         │               │    标签集        │
│  (左上树状)      │   待选指标      │   (右上树状)      │
│                 │  (中间核心)     │   同级并列多选     │
│   (可展开)       │               │                 │
│                 │               │    规则          │
│                 │               │   (右下树状)      │
│                 │               │   (多对多+参数)    │
└─────────────────┴───────────────┴─────────────────┘
```

- **左上**：指标树（无限层级，指标本身即节点，一对一归属）
- **中间**：待挂靠指标池（未挂载到任何目标的指标）
- **右上**：标签集树（无限层级，同级并列显示，多对多关联）
- **右下**：规则树（无限层级，多对多关联，挂靠后需设置参数）
- **Header**：部门切换器（头像栏下拉，默认"财务部"）

---

## 二、已确认的基础交互

### 连线式挂靠
1. 点击待挂靠指标 → 进入"连线模式"
2. SVG 虚线 + 箭头从指标指向鼠标位置，实时跟随
3. 鼠标悬停目标节点时，节点高亮
4. 按 **空格键** 确认挂靠
5. 按 **ESC** 取消

### 分权分域
- 部门切换器在 Header 头像栏
- 指标树和标签集随部门切换
- 规则全局不变
- 待选指标全局可见

---

## 三、需要深度调研的 12 个问题

### 🔹 问题 1：连线式挂靠的完整 UX 流程

**已确认**：点击指标 → 连线跟随 → 空格确认 → ESC 取消

**待细化**：
- 进入连线模式后，页面其他区域是否变暗（focus mask）？变暗程度多少？
- 连线过程中，无效目标区域是否需要灰显/禁用？
- 有效目标区域的 hover 反馈：边框高亮？背景色变化？脉冲动画？
- 挂靠成功后，源指标是否从"待挂靠"区域消失？消失动画（淡出/缩小/飞入目标）？
- 挂靠成功后，目标节点是否需要短暂脉冲/闪烁反馈？持续多久？
- 如果用户误触空格（未悬停目标时），是无操作静音处理，还是 toast 提示"请选择目标"？
- 连线模式下是否允许滚动页面？滚动时连线如何跟随？
- 连线的视觉样式：虚线？实线？颜色（深色主题下）？粗细？箭头大小？
- 同时只能有一条连线在进行吗？还是支持多指标同时连线？

**调研方向**：Figma 连线、Miro 连接线、Linear 关系连线、Blender 节点编辑器

---

### 🔹 问题 2：标签集树的同级并列多选交互

**需求**：标签集是树状结构，同一层级下的标签在一行并列显示（类似标签云但有层级），支持多选。

**已确认**：
- 空格 = 选中/取消选中当前标签
- Ctrl+Alt+Shift+空格 = 全选当前层级所有标签并退出

**待细化**：
- 同级标签一行并列时，超出宽度如何处理？换行？横向滚动？折叠为"+N"？
- 已选中的标签在视觉上如何区分？背景色？边框？勾选图标？文字颜色？
- 多选模式下，是否需要在面板顶部显示"已选 N 个"计数器？是否需要"清空选择"按钮？
- 标签树展开/收起时，同级标签行的动画（高度变化、位置偏移）怎么做才自然？
- 选中父标签时，子标签是否自动全选？还是保持独立？
- 标签的父子层级缩进如何设计？线条连接？纯缩进？不同背景色区分层级？
- 深色主题下，标签选中的高亮色（避免刺眼）：蓝色？青色？绿色？
- 标签是否支持颜色自定义？（如"核心指标"标签红色，"考核"标签黄色）

**调研方向**：VS Code 多文件选择、Figma 组件多选、Notion 标签多选、Arc 浏览器标签分组

---

### 🔹 问题 3：规则树的参数设置交互

**需求**：规则是树状呈现，指标挂靠到规则后需要设置具体参数。

**规则类型示例**：
- 阈值告警：上限值、下限值、单位、告警级别
- 波动检测：算法（绝对值/百分比/同比环比）、阈值、时间窗口
- TOPN 监控：N 值、排序维度、阈值

**待细化**：
- 挂靠后，参数设置的最佳交互方式？
  - A. 弹窗 Modal（空间大但打断感强）
  - B. 右侧滑出抽屉 Drawer（空间充足，不离开当前页面）
  - C. 节点展开内嵌表单（紧凑，但参数多时会拥挤）
  - D. 底部固定面板（类似属性面板，随时可编辑）
- 如果规则本身有层级（如"阈值规则"下分"上限规则"和"下限规则"），参数如何继承/覆盖？
- 参数输入的控件选择：数值用滑块还是输入框？枚举用下拉还是单选？布尔用 Switch？
- 参数验证（如阈值上限必须大于下限），实时验证还是提交时验证？验证错误的视觉反馈？
- 参数设置完成后，规则节点上如何显示参数摘要？（如"阈值: 80-120"）
- 深色主题下，表单控件的视觉设计（输入框背景、焦点环颜色、错误状态色）

**调研方向**：Retool 属性面板、Framer 组件参数、AWS CloudWatch 告警配置、DataDog 监控规则

---

### 🔹 问题 4：删除挂靠的操作方式

**场景**：用户需要删除已建立的关联关系。

- 删除指标树挂靠 = 该指标回到"待挂靠"区域（清空 treeParentId）
- 删除标签集挂靠 = 从该指标的 tagIds 中移除对应标签
- 删除规则挂靠 = 从该指标的 ruleIds 中移除对应规则

**候选方案**：
- A. 右键目标节点 → 显示"已挂靠指标"子菜单 → 点击移除
- B. Hover 目标节点时显示已挂靠数量徽章 → 点击展开列表 → 逐个点删除
- C. 点击已存在的连线（指标→节点之间的线）→ 显示删除按钮 → 确认删除
- D. 在"待挂靠"区域显示一个"回收站"面板 → 拖拽已挂靠指标到回收站解除关联
- E. 选中指标后，高亮显示其所有关联连线 → 点击某条连线上的删除图标

**待细化**：
- 删除操作是否需要二次确认？（尤其是规则挂靠，可能配置了复杂参数）
- 删除后是否有撤销机制？（如 toast 提示"已删除 [撤销]")?
- 批量删除：能否一次性删除一个指标的所有关联？
- 删除指标树中的父节点时，子节点（指标）是否一并回到待挂靠？还是子节点独立保留？

**调研方向**：Figma 图层关联删除、Notion 关系属性删除、Jira 链接删除、Miro 连接线删除

---

### 🔹 问题 5：树状界面的人性化设计

**客户明确要求**：界面要非常友好，不要传统列表化管理那种繁琐感。

**待细化**：
- 树节点的展开/收起动画：折叠时高度渐变？旋转箭头图标？子节点淡入？
- 展开/收起动画时长建议（多少毫秒最自然？）
- 空状态设计：
  - 待选指标为空时 → 显示什么？插画？文字提示？"暂无待挂靠指标"？
  - 指标树为空时 → "暂无指标，点击 + 添加"
  - 标签集为空时 → 同上
  - 规则为空时 → 同上
- 搜索过滤时，树如何动态折叠/展开以显示匹配节点？未匹配节点是否灰显？
- 节点 hover 时的视觉反馈：阴影？边框？背景色渐变？
- 节点选中状态（点击后）的视觉反馈：和 hover 如何区分？
- 深色主题下，树节点的高亮色如何选择？避免刺眼。推荐色值？
- 节点的圆角、间距、字体大小等微观设计（视觉层级如何建立？）
- 树中是否显示节点深度指示器（竖线/缩进/虚线连接）？

**调研方向**：Arc 浏览器标签页树、Linear 侧边栏、Raycast 命令面板、Craft 文档大纲

---

### 🔹 问题 6：指标卡片设计

**中间区域展示待挂靠的指标**。

**待细化**：
- 卡片上展示哪些信息？名称、编码、来源、状态？是否需要显示"一级/二级"分类？
- 卡片尺寸：固定宽高？自适应？网格布局（几列）？
- 卡片 hover 时显示更多信息（tooltip/展开）？还是直接显示全部？
- 已挂靠的指标是否从"待挂靠"区域消失？还是保留但显示"已挂靠"标记？
  - 消失：空间更清爽，但用户可能找不到刚挂靠的指标
  - 保留+标记：信息完整，但列表会变长
- 卡片的视觉样式：纯色背景？渐变？边框？阴影？
- 深色主题下卡片背景色推荐（与页面背景区分但不突兀）
- 卡片是否支持拖拽排序（待挂靠区域内的排序）？
- 卡片的选中状态（点击后进入连线模式）如何视觉区分？

**调研方向**：Notion 数据库卡片、Airtable 卡片、Linear 列表项、Figma 组件卡片

---

### 🔹 问题 7：深色主题的具体色值方案

**需求**：演示优先用深色（蓝黑），画布图表颜色跟随主题联动。

**待细化**：
- 页面背景色：纯黑？深蓝黑？偏灰的暗色？推荐具体色值。
- 卡片背景色：比页面背景稍亮的深色？带微弱透明度？推荐色值。
- 主文字色：纯白？偏灰的白？不同层级的文字颜色（标题/正文/辅助文字）？
- 高亮色/强调色：蓝色？青色？紫色？用于选中状态、hover 状态、连线。推荐色值。
- 边框/分割线颜色：半透明灰？微弱蓝色？推荐色值。
- 错误/警告/成功状态色：深色主题下的红/黄/绿如何不刺眼？
- 树节点的层级颜色：是否需要不同层级用不同深浅的背景色区分？
- 连线颜色：悬停时和确认后的颜色是否不同？
- 是否需要一套完整的 CSS 变量定义？

**调研方向**：VS Code Dark+ 主题、GitHub Dark Dimmed、Linear 深色模式、Raycast 深色主题

---

### 🔹 问题 8：树节点维护的完整交互

**需求**：用户需要增删改树节点，以及拖拽调整层级。

**已确认**：
- 添加节点：面板标题栏 "+" 按钮 → 弹窗输入名称
- 编辑名称：inline 编辑
- 删除节点：hover 显示删除按钮

**待细化**：
- 添加根节点 vs 添加子节点：统一一个 "+" 按钮，弹窗时选择"添加为根"还是"添加为子节点"？还是两个独立按钮？
- Inline 编辑的触发方式：单击名称？双击？点击编辑图标？
- Inline 编辑的控件：直接变成输入框？还是弹出小型输入框？
- 编辑完成后如何确认？Enter 保存？失去焦点自动保存？Esc 取消？
- 删除节点时的确认策略：
  - 空节点（无子节点、无挂靠指标）→ 直接删除？
  - 有子节点 → 提示"子节点将一并删除"？
  - 有挂靠指标 → 提示"N 个指标将回到待挂靠区域"？
- **拖拽调整层级**（用户明确要求一起做）：
  - 拖拽节点到另一个节点上 = 改变 parentId
  - 拖拽时是否显示放置区域高亮？目标节点的视觉反馈？
  - 拖拽到根级别（parentId = undefined）如何触发？拖到面板空白区域？
  - 拖拽时的 ghost/预览图像：原节点半透明？还是显示节点名称的浮动标签？
  - 拖拽动画：释放后节点如何"飞入"新位置？
  - 是否允许跨面板拖拽？（如把指标树节点拖到标签集？）

**调研方向**：VS Code 文件树拖拽、Notion 页面树拖拽、Figma 图层拖拽、Finder 文件夹拖拽

---

### 🔹 问题 9：搜索与过滤

**待细化**：
- 搜索框放在哪里？每个面板顶部？还是全局搜索在 Header？
- 搜索范围：只搜待选指标？还是同时搜树节点、标签、规则？
- 搜索时，树如何动态响应？自动展开匹配的节点？未匹配节点灰显？
- 搜索结果高亮：匹配文字变色？背景高亮？
- 是否支持高级过滤（按来源、按状态、按部门）？
- 搜索历史/最近搜索是否保存？
- 空搜索结果的状态设计？

**调研方向**：VS Code 全局搜索、Notion 搜索、Raycast 搜索、Figma 图层搜索

---

### 🔹 问题 10：性能与大数据量处理

**待细化**：
- 待选指标数量较多时（如 100+），中间区域如何渲染？
  - 虚拟滚动？（技术复杂度中）
  - 分页加载？（打断操作流）
  - 全部渲染？（100 个卡片在 React 下性能如何？）
- 树的层级很深时（如 10 层），展开性能如何？是否需要懒加载子节点？
- 连线动画的 SVG 性能：大量连线同时存在时是否卡顿？
- localStorage 数据量上限：每个部门存储多少条数据是安全的？

**调研方向**：React 虚拟滚动库（react-window、react-virtuoso）、SVG 性能优化、localStorage 容量限制

---

### 🔹 问题 11：动画与微交互汇总

**客户要求"人性化、操作简单"**，动画是提升体验的关键。

**待梳理的动画清单**：

| 场景 | 动画类型 | 建议时长 |
|------|---------|---------|
| 节点展开/收起 | 高度渐变 + 子节点淡入 | ? |
| 节点添加 | 缩放出现（0→1）+ 淡入 | ? |
| 节点删除 | 缩放消失（1→0）+ 高度收缩 | ? |
| 连线出现 | 路径绘制（stroke-dashoffset） | ? |
| 连线消失 | 淡出 | ? |
| 指标卡片 hover | 轻微上浮/阴影增强 | ? |
| 指标卡片选中（进入连线模式） | 脉冲边框/放大 | ? |
| 挂靠成功 | 源卡片缩小消失 + 目标节点脉冲 | ? |
| 部门切换 | 内容淡出淡入 | ? |
| 搜索过滤 | 未匹配节点淡出（透明度变化） | ? |
| 空状态出现 | 插画淡入 | ? |

**待细化**：
- 整体动画风格：柔和（ease-out）？弹性（spring）？线性？
- 是否使用 Framer Motion？（shadcn/ui 生态推荐）还是纯 CSS transition？
- 动画时长建议：整体偏快（<200ms）还是偏慢（300-400ms）？

---

### 🔹 问题 12：快捷键与操作效率

**已确认**：
- 空格 = 确认挂靠 / 选中标签
- ESC = 取消连线模式
- Ctrl+Alt+Shift+空格 = 全选标签

**待补充**：
- 删除节点快捷键？（Delete / Cmd+Delete）
- 重命名节点快捷键？（F2 / Enter）
- 展开/收起节点快捷键？（→ / ←）
- 搜索聚焦快捷键？（Cmd+K / Cmd+F）
- 撤销操作快捷键？（Cmd+Z）
- 是否需要 Vim 风格的导航？（j/k 上下移动，h/l 展开收起）

**调研方向**：VS Code 快捷键、Linear 快捷键、Notion 快捷键、Figma 快捷键

---

## 四、调研交付物要求

针对每个问题，请提供：

1. **推荐方案**（首选，含理由）
2. **备选方案**（次选，说明为什么不首选）
3. **交互流程图**（文字版 step-by-step）
4. **视觉设计建议**（颜色 hex 值、间距 px、圆角、阴影、动画时长）
5. **实现复杂度评估**（高/中/低）
6. **参考截图/链接**（如有）

---

## 五、参考素材

- **重构文档**：`C:\Users\pan47\Desktop\0606 demo大重构\# 0606 平台核心功能简化与页面重构.md`
- **当前代码**：`C:\Users\pan47\Desktop\Demo v2\src\`
- **v1 参考代码**（血缘画布 SVG 实现）：`C:\Users\pan47\Desktop\Demo v2\src\pages\lineage\LineageCanvasPage.tsx`
- **技术栈**：React 19 + Tailwind CSS + shadcn/ui + Framer Motion（推荐）
